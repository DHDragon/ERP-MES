server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/erp_dev?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379

erp:
  jwt:
    secret: "erp-dev-secret-please-change-to-32bytes-min"
    expire-seconds: 3600
